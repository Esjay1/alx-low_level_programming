#include "main.h"

/**
 * add -display the sum of two integers
 * @a: The first integer
 * @b: the second integer
 *
 * Return: the sum of the numbers
 */
int add(int a, int b)
{
	int sum;

	sum = (a + b);
	return (sum);
}
