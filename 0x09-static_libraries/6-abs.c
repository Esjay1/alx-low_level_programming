#include "main.h"

/**
 * _abs - displays the absolute value of an int
 * @c: The integer used as argument
 *
 * Return: int if int >= 0
 * and int if int < 0.
 */
int _abs(int c)
{
	if (c >= 0)
	{
		return (c);
	}
	else
		return (-c);
	return (-c);
}
