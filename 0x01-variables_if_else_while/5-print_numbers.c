#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int tester;  /* declare the variables */

	for (tester = 0; tester <= 9; tester++)
	{
		printf("%d", tester);
	}
	printf("\n");
	return (0);
}
