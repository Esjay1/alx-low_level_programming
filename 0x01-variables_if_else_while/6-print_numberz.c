#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int tester;  /* declare the variable */

	for (tester = 48; tester <= 57; tester++)
	{
		putchar(tester);
	}
	putchar('\n');
	return (0);
}
